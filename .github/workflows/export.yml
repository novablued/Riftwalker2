name: Export Nova Web
on: [workflow_dispatch]

jobs:
  export:
    runs-on: ubuntu-latest
    container:
      image: barichello/godot-ci:4.2.1
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show export_presets.cfg
        run: cat nova/export_presets.cfg

      - name: Import
        run: |
          mkdir -p nova/.godot
          godot --headless --path nova --editor --quit

      - name: Export Web
        run: |
          mkdir -p nova/build/web
          godot --headless --path nova --export-release "web" build/web/index.html

      - name: List files
        run: ls -la nova/build/web

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Godot Web Export
          path: nova/build/web
